{
  "config": {
    "step": {
      "user": {
        "title": "Set up Chores & Maintenance",
        "description": "This creates the hub. Add chores and set up notifications afterward from the hub's Configure button."
      }
    },
    "abort": {
      "single_instance_allowed": "Only one Chores & Maintenance hub is allowed."
    }
  },
  "options": {
    "step": {
      "init": {
        "menu_options": {
          "add_chore": "Add a chore",
          "remove_chore": "Remove a chore",
          "notify_mapping": "Set up who gets notified"
        }
      },
      "add_chore": {
        "title": "Add a chore",
        "data": {
          "name": "Name",
          "mode": "Due-trigger type",
          "interval_days": "Interval (days) — only used if type is Interval",
          "cycle_threshold": "Cycle threshold — only used if type is Cycle count",
          "nfc_enabled": "Complete by scanning an NFC tag",
          "nfc_tag_entity_id": "NFC tag — required if NFC tag scanning is enabled",
          "notification_enabled": "Complete via a notification action button",
          "notify_enabled": "Send a repeating reminder notification",
          "notify_time": "Reminder notification time — required if reminders are enabled",
          "message": "Notification message — leave blank for the default: the chore's name plus \"is due.\""
        }
      },
      "remove_chore": {
        "title": "Remove a chore",
        "data": {
          "chore_id": "Chore"
        }
      },
      "notify_mapping": {
        "title": "Set up who gets notified"
      }
    },
    "abort": {
      "no_chores": "There are no chores to remove yet."
    },
    "error": {
      "interval_days_required": "Enter an interval (in days) for this chore.",
      "cycle_threshold_required": "Enter a cycle threshold for this chore.",
      "completion_method_required": "Enable at least one completion method: NFC tag or notification action.",
      "nfc_tag_required": "Select an NFC tag, since NFC tag scanning is enabled.",
      "notify_time_required": "Set a reminder notification time, since reminders are enabled."
    }
  }
}
